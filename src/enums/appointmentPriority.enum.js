export const AppointmentPriority = Object.freeze({
  URGENT: 1,
  NORMAL: 2,
});

export const AppointmentPriorityMap = Object.freeze({
  urgent: AppointmentPriority.URGENT,
  normal: AppointmentPriority.NORMAL,
});
